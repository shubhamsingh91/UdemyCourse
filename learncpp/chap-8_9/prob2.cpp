/*Write a C++ program that implements a function template to sort a given array of any data type in descending order. 
The program should:
Define a function template called sort_descending that takes an array of any data type and its size as 
parameters and sorts the array in descending order.
In the main function, declare two arrays of different data types (e.g., int and double),
 initialize them with some values, and call the sort_descending function for each array to sort them in descending order.
Display the sorted arrays on the screen with appropriate messages.
*/

#include <iostream>
#include <stdio.h>
using namespace std;

template<typename T>
void sort_descending(T* arr, int size){


    
}

int main(){


    return 0;
}